springdoc:
  swagger-ui:
    path: /swagger-ui.html

server:
  port: 5000
  address: 0.0.0.0

spring:
  profiles:
#    active: ${SPRING_PROFILES_ACTIVE:postgres}
     active: h2
  application:
    name: test-management-api
  
  
    
logging:
  level:
    com.testmanagement: INFO
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
